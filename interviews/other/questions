Segregate 0s and 1s in an array
Solution: https://www.geeksforgeeks.org/segregate-0s-and-1s-in-an-array-by-traversing-array-once/
Markov chain random sentence generator - group_id, contents, children dictionary

Given an array of Functions, Event name (started/stopped) and timestamp (integer), find the inclusive execution time and exclusive execution time. 
Build tic-tac-toe, one player vs computer

maximum alphabetical substring
twitter authentication tokens expiry commands
SELECT house.BUYER_ID, SUM(price.PRICE) AS TOTAL_WORTH FROM house left outer join price on house.HOUSE_ID = price.HOUSE_ID GROUP BY house.BUYER_ID HAVING TOTAL_WORTH >= 100 AND COUNT(house.HOUSE_ID) > 1;