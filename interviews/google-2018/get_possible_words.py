'''
Given a word, find a list of all possible words from a dictionary which contain letters from the input word
Assumptions:
Only contains letters A to Z
Letters are case insensitive
'''
def getWords(initial_word):
	letters = initial.split()
	words = []
	words.append(initial)
	if words_list()
	getWordsHelper(words, letters, k)

def getWordsHelper(words,letters,k):
	def getWord():
		result = ""
		for l in letters:
			result += l
		return result
	# base case
	if len(letters) == 0:
		return
	getWordsHelper()

# helper method which evaluates word against a dictionary
def validate(word):
	pass